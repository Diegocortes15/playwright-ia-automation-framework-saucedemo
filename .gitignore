node_modules/
test-results/
playwright-report/
blob-report/
playwright/.cache/

auth/*.json
!auth/.gitkeep

.env
.env.local

# Claude Code: project config committed; per-developer settings ignored
.claude/settings.local.json

# Playwright CLI workspace (per-session browser state, console logs, snapshots)
.playwright-cli/
