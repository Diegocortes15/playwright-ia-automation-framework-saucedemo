node_modules/
test-results/
playwright-report/
blob-report/
playwright/.cache/

auth/*.json
!auth/.gitkeep

.env
.env.local

# Claude Code: project config committed; per-developer settings ignored
.claude/settings.local.json

# Playwright CLI workspace (per-session browser state, console logs, snapshots)
.playwright-cli/

# Skill experimentation area (created by /scaffold-page-object and similar)
scratch/

# Temporary tsconfig used by /scaffold-page-object step 11 (isolated typecheck);
# the workflow rms it after every run, but ignore it defensively in case of crash
.tsconfig.scratch.json
