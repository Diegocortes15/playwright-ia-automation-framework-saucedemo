# Make the `engines` field in package.json a gate, not a suggestion.
# Without this npm only warns, so a contributor on the wrong Node major finds out when CI
# disagrees with their machine rather than at install time. Both workflows pin node 22.
engine-strict=true
